<Window
    x:Class="FastFluentFilesFolders.Views.MainWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FastFluentFilesFolders.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:FastFluentFilesFolders.ViewModels"
    xmlns:views="using:FastFluentFilesFolders.Views"
    xmlns:ctrls="using:Microsoft.UI.Xaml.Controls"
	xmlns:CTKTCtrls="using:CommunityToolkit.WinUI.Controls" 
    mc:Ignorable="d">
    
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <ctrls:NavigationView x:Name="MainNav" PaneDisplayMode="LeftCompact"
                          IsBackButtonVisible="Collapsed"
                          IsSettingsVisible="False"
                          SelectionChanged="OnNavSelectionChanged">
        <ctrls:NavigationView.PaneHeader>
            <StackPanel Orientation="Horizontal" Spacing="4">
                <FontIcon Glyph="&#xE838;" FontSize="16" />
                <TextBlock Text="FastFluentFilesFolders" FontSize="14" FontWeight="SemiBold"
                           VerticalAlignment="Center" Margin="4,0,0,0"/>
            </StackPanel>
        </ctrls:NavigationView.PaneHeader>

        <ctrls:NavigationView.MenuItems>
            <ctrls:NavigationViewItem Icon="Folder" Content="文件浏览" Tag="explorer"/>
            <ctrls:NavigationViewItem Icon="AllApps" Content="插件" Tag="plugins"/>
        </ctrls:NavigationView.MenuItems>

        <ctrls:NavigationView.PaneFooter>
            <ctrls:NavigationViewItem x:Name="SettingsNavItem" Icon="Setting" Content="设置" Tag="settings" Tapped="OnSettingsNavTapped"/>
        </ctrls:NavigationView.PaneFooter>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TitleBar x:Name="AppTitleBar"
                  Title="FastFluentFilesFolders" Subtitle="v1.0.0-β">
                <TitleBar.IconSource>
                    <FontIconSource Glyph="&#xE838;"/>
                </TitleBar.IconSource>
            </TitleBar>

            <!-- Explorer Area: pre-loaded, visibility toggled (no animation to avoid lag) -->
            <views:ExplorerPage x:Name="ExplorerArea" Grid.Row="1" Visibility="Visible" />

            <!-- Frame for plugins/settings: lightweight pages with animation -->
            <Frame x:Name="ContentFrame" Grid.Row="1" Visibility="Collapsed">
                <Frame.ContentTransitions>
                    <TransitionCollection>
                        <NavigationThemeTransition />
                    </TransitionCollection>
                </Frame.ContentTransitions>
            </Frame>
        </Grid>
    </ctrls:NavigationView>
</Window>
