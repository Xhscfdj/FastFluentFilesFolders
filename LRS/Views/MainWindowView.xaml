<Window
    x:Class="LRS.Views.MainWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LRS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:LRS.ViewModels"
    xmlns:views="using:LRS.Views"
	xmlns:CTKTCtrls="using:CommunityToolkit.WinUI.Controls" 
    mc:Ignorable="d">
    
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid>
        <Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
			<ColumnDefinition Width="20" />
			<ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>
        <TitleBar x:Name="AppTitleBar"
                  Grid.ColumnSpan="2"
              Title="LRS" Subtitle="v0.0.0-dev">
            <TitleBar.IconSource>
				<FontIconSource Glyph="&#xE838;"/>
            </TitleBar.IconSource>
        </TitleBar>
        <views:FileTreeView Grid.Row="1" Grid.Column="0" />
		<CTKTCtrls:GridSplitter Grid.Column="1" Grid.RowSpan="2"/>
		<views:MiddleFilesView Grid.Row="1" Grid.Column="2"/>
    </Grid>

</Window>
