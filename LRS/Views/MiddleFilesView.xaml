<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LRS.Views.MiddleFilesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LRS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:LRS.ViewModels"
    xmlns:uc="using:LRS.UserControls"
    d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <uc:TreeDataGrid x:Name="FileGrid" Grid.Row="0"
                         ItemsSource="{Binding CurrentFolderContent}"
                         ParentRowHeight="{Binding AppConfigs.MiddleFilesHeight}"
                         IsSpecialFolder="{Binding IsCurrentFolderSpecial}"
                         ItemInvoked="OnTreeDataGridItemInvoked"
                         Margin="0,10,0,0"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Padding="20,10,20,10">
            <TextBlock Text="{Binding CurrentFolderContent.Count}" Foreground="#D6D6D6"/>
            <TextBlock Text="个项目" Foreground="#D6D6D6"/>
        </StackPanel>
    </Grid>
</Page>
