<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LRS.Views.MiddleFilesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LRS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tv="using:WinUI.TableView"
    xmlns:vm="using:LRS.ViewModels" 
    d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
    x:Name="MFV"
    mc:Ignorable="d">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<!--<Button Content="Click to change the testString." Command="{Binding testFunctionCommand}"/>-->
		<tv:TableView ItemsSource="{Binding CurrentFolderContent}"
              Grid.Row="0"
              RowHeight="{Binding AppConfigs.MiddleFilesHeight}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              SelectionMode="Single"
              SelectionUnit="Row"
              GridLinesVisibility="None"
              Margin="0,10,0,0">
			<tv:TableView.Columns>
				<tv:TableViewTemplateColumn Header="名称 " Width="5*" CanSort="True" SortMemberPath="Name">
					<tv:TableViewTemplateColumn.CellTemplate>
						<DataTemplate>
							<Grid DoubleTapped="OnRowDoubleTapped">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="30"/>
									<ColumnDefinition Width="*"/>
								</Grid.ColumnDefinitions>
								<Image Source="{Binding Icon}" Width="20" Height="20"/>
								<TextBlock Text="{Binding Name}" VerticalAlignment="Center" Grid.Column="1"/>
							</Grid>
						</DataTemplate>
					</tv:TableViewTemplateColumn.CellTemplate>
				</tv:TableViewTemplateColumn>
				
				<tv:TableViewTemplateColumn Header="修改日期" Width="2*" CanSort="True" SortMemberPath="LastModifiedTime">
					<tv:TableViewTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock DoubleTapped="OnRowDoubleTapped" Text="{Binding LastModifiedTimeString}" VerticalAlignment="Center"/>
						</DataTemplate>
					</tv:TableViewTemplateColumn.CellTemplate>
				</tv:TableViewTemplateColumn>

				<tv:TableViewTextColumn Header="创建日期" Width="2*" CanSort="True" SortMemberPath="FirstCreatedTime" Binding="{Binding FirstCreatedTimeString}"/>

				<tv:TableViewTemplateColumn Header="大小" Width="*" CanSort="True" SortMemberPath="ExactSize">
					<tv:TableViewTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock DoubleTapped="OnRowDoubleTapped" Text="{Binding VisualSize}" TextAlignment="Right" VerticalAlignment="Center"/>
						</DataTemplate>
					</tv:TableViewTemplateColumn.CellTemplate>
				</tv:TableViewTemplateColumn>
			</tv:TableView.Columns>
		</tv:TableView>
	</Grid>
</Page>
<!--<tv:TableView ItemsSource="{Binding CurrentFolderContent}"
              Grid.Row="1"
			  HeadersVisibility="All"
              RowHeight="80"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              SelectionMode="Single"
              SelectionUnit="Row"
              GridLinesVisibility="None"
              HeaderGridLinesVisibility="None"
              Margin="0,10,0,0">
			<tv:TableView.HeaderTemplate>
				<DataTemplate>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="10"/>
							<ColumnDefinition Width="30"/>
							<ColumnDefinition Width="5*"/>
							<ColumnDefinition Width="2*"/>
							<ColumnDefinition Width="2*"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>
						<TextBlock Grid.Column="2" Text="名称" FontWeight="Bold"/>
						<TextBlock Grid.Column="3" Text="修改日期" FontWeight="Bold"/>
						<TextBlock Grid.Column="4" Text="创建日期" FontWeight="Bold"/>
						<TextBlock Grid.Column="5" Text="大小" FontWeight="Bold" TextAlignment="Right"/>
					</Grid>
				</DataTemplate>
			</tv:TableView.HeaderTemplate>
			<tv:TableView.ItemTemplate>
				<DataTemplate>
					<Grid DoubleTapped="OnRowDoubleTapped" Height="{Binding DataContext.AppConfigs.MiddleFilesHeight,ElementName=MFV}">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="10"/>
							<ColumnDefinition Width="30"/>
							<ColumnDefinition Width="5*"/>
							<ColumnDefinition Width="2*"/>
							<ColumnDefinition Width="2*"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>

						<Image Source="{Binding Icon}" Grid.Column="1" Width="20" Height="20"/>
						<TextBlock Text="{Binding Name}" Grid.Column="2" VerticalAlignment="Center"/>
						<TextBlock Text="{Binding LastModifiedTimeString}" Grid.Column="3" VerticalAlignment="Center"/>
						<TextBlock Text="{Binding FirstCreatedTimeString}" Grid.Column="4" VerticalAlignment="Center"/>
						<TextBlock Text="{Binding VisualSize}" TextAlignment="Right" Grid.Column="5" VerticalAlignment="Center"/>
					</Grid>
				</DataTemplate>
			</tv:TableView.ItemTemplate>
		</tv:TableView>-->